# Round 4.5: Web-to-CLI Handoff Buttons

4 tasks. Fix the broken "Try in Claude Code" button and add a CLI handoff to the deep-dive modal. All changes must pass `pnpm build`.

---

- [x] **Task 1: Fix FeedCard copy button** — In `src/components/feed-card.tsx`: (1) Add `userInput?: string` to the component's props type (around line 19-25, add it after `defaultExpanded`). (2) Change the clipboard copy on line 93 from `claude /lenny "${item.title}"` to a properly formatted command. Build the copy string like this: start with `claude /lenny-learn "`, then add `item.filename`, then if `userInput` exists and is not empty, add ` | ` followed by a sanitized version of userInput (replace all double-quote characters `"` with single-quote `'`, and truncate to 200 characters — if truncated, append `...`), then close with `"`. So the final copied text looks like: `claude /lenny-learn "newsletters/pricing-your-saas-product.md | I'm a B2B founder at Series A..."` or just `claude /lenny-learn "newsletters/pricing-your-saas-product.md"` if no userInput. (3) Change the button label from "Try in Claude Code" to "Learn this in Claude Code". Also update the setTimeout reset text on line 96 to match.

- [x] **Task 2: Pass userInput to FeedCard** — In `src/app/results/page.tsx`, find where `<FeedCard>` is rendered (around line 342-350) and add the prop `userInput={userInput}`. The `userInput` variable already exists in the component scope — it's decoded from URL params earlier in the file.

- [x] **Task 3: Add CLI button to deep-dive modal** — In `src/components/deep-dive-modal.tsx`, in the `<DialogFooter>` section (around line 112-133), add a copy-to-clipboard button BEFORE the existing YouTube and Newsletter links. Label: "Learn interactively in Claude Code". When clicked, it copies to clipboard: `claude /lenny-learn "${filename} | ${sanitizedUserInput}"` using the same sanitization as Task 1 (replace `"` with `'`, truncate to 200 chars). Show "Copied!" feedback for 2 seconds — use the same useState pattern as the feed-card button (change button text, setTimeout to reset). Style it as a small bordered button matching the existing link style in the footer. The modal already receives both `filename` and `userInput` as props — no new wiring needed.

- [x] **Task 4: Final build check** — Run `pnpm build` and verify it passes with zero errors. Check that: (1) FeedCard now accepts and uses `userInput` prop and copies the correct `claude /lenny-learn` format with filename and pipe-delimited context, (2) results page passes `userInput` to FeedCard, (3) deep-dive modal footer has the new "Learn interactively" copy button. If build fails, fix the errors. This task is only done when build passes clean.
